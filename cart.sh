source common.sh

# declaring variable for the component Cart
component=cart

# declaring variable so that schema loading commands are skipped "false", by using "if" condition
schema_load=false

# to keep the code DRY we sholud remove repetion, so the solution is using "Functions"
NODEJS